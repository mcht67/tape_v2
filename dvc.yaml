# This file defines the dvc stages and its depencies

stages:
  #load_dataset:
  #   cmd:
  #     ${python.base} source/load_dataset.py
  #   deps:
  #     - source/load_dataset.py
  #   params:
  #     - dataset.config
  #     - dataset.huggingface_path
  #     - path.dataset_metadata
  #   outs:
  #     - ${path.dataset_metadata}

  # embed_audio_with_perch:
  #   cmd: ${python.perch} source/embed_audio.py
  #   deps:
  #     - source/embed_audio.py
  #     - ${path.dataset_metadata}
  #   params:
  #     - embeddings
  #   outs:
  #     - ${path.perch_embeddings_metadata}

  # embed_audio_with_birdset:
  #   cmd: ${python.train} source/torch_embed_audio.py
  #   deps:
  #     - source/torch_embed_audio.py
  #     - ${path.dataset_metadata}
  #   params:
  #     - embeddings
  #   outs:
  #     - ${path.birdset_embeddings_metadata}

  # add_labels:
  #   cmd: ${python.base} source/add_labels.py
  #   deps:
  #     - source/add_labels.py
  #     - ${path.dataset_metadata}
  #   params:
  #     - path.dataset
  #     - labels
  #   outs:
  #     - ${path.added_labels_metadata}
  #     - ${path.dataset}: 
  #         persist: true
  #         cache: false

  # upload_dataset:
  #   cmd: ${python.base} source/upload_dataset.py
  #   deps:
  #     - source/upload_dataset.py
  #     - ${path.dataset}
  #   params:
  #     - path.dataset
  #     - dataset.config

  train:
    cmd: ${python.complete} source/train.py #${python.train} source/train.py
    deps:
      - source/train.py
      #- ${path.dataset}
    params:
      - general.random_seed
      - dataset
      - model
      - objectives
      - train
      - log
    outs:
      # - ${path.val_dataset}:
      #     push: false        # cached locally, never pushed to remote
      - ${path.train_output}:
          persist: true      # pushed to remote
      # - ${path.train_log_dir}:
      #     persist: true      # pushed to remote
      # - ${path.checkpoint_dir}:
      #     persist: true      # pushed to remote

  
  evaluate:
    cmd: ${python.complete} source/evaluate_on_test_split.py
    deps: 
      - source/evaluate_on_test_split.py
      - dataset
      - model
      - objectives
      - train
    outs:
      - ${path.eval_output}:
          persist: true      # pushed to remote